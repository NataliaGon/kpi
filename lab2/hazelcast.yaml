hazelcast:
  cluster-name: dev
  network:
    join:
      multicast:
        enabled: false
      tcp-ip:
        enabled: true
        member-list:
          - hz1
          - hz2
          - hz3
  cp-subsystem:
    cp-member-count: 3
    group-size: 3
